#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name = 'SnsManager',
    version = '0.5.0',
    description = 'Social Network Service Manager',
    license='BSD',
    author='Waveface, Inc',
    author_email='support@waveface.com',
    url = 'http://github.com/waveface/SnsManager',
    packages = find_packages(),
    keywords='SnsManager',
    zip_safe=True,
)
